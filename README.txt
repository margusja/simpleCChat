1. gcc simpleCChat.c -i simpleCChat // for compailing
2. ./simpleCChat 2020 // Now server should run in port 2020

3. Clients can connect using "telnet [IP] 2020" Client can just type into the telnet interface. All that client submits are transfered to anoters clients telnet console. 
